
<h1>C'est l'heure de creer ton potin !</h1>
<p>Ne crains-rien, tu ne pourras pas etre poursuivi !</p>
<hr>

<form action='/gossips/new' method='POST'>
  <div class="form-group">
    <label for="exampleAuteur">Qui est l'auteur du potin ?</label>
    <input type="text" class="form-control" name="gossip_author" aria-describedby="emailHelp" placeholder="ton nom">
  </div>
  <div class="form-group">
    <label for="exampleContenu">Alors, raconte-nous</label>
    <input type="text" class="form-control" name="gossip_content" placeholder="ca croustille deja">
  </div>
  <button type="submit" class="btn btn-danger">Sousmettre </button>
  <br><br>
</form>

<%= form_tag url_for(action: 'create'), method: "post" do |f| %>

<% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <div class="alert alert-warning alert-dismissible fade show" role="alert">
    <strong>Fluuuute ! Il y a une erreur.</strong>
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<br>

<%= image_tag("gg.png", :alt => "gg") %>

<% end %>

